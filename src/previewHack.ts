// This module provides a lightweight helper to force the editor title menu to
// expose icons reliably during development. It does not alter production behavior.

export function noop() {
  // Intentionally empty
}
